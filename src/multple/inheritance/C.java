package multple.inheritance;

/*In below you don't have to override print() method because
* B has already done it, so in C you don't have to override, even though you're still implementing A
* */
public class C extends B implements A{
}
